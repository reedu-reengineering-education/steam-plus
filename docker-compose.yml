version: '3.9'

services:
  postgis:
    image: postgis/postgis:14-3.2
    ports:
      - '5432:5432'
    restart: on-failure
    healthcheck:
      test: 'exit 0'
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
